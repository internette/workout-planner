// The page is a client component (its example keeps state), so its tab title is set here.
export const metadata = { title: 'Progress bar — Design system' };

export default function ProgressBarLayout({ children }: { children: React.ReactNode }) {
  return children;
}
