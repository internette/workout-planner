// The page is a client component (its examples keep state), so its tab title is set here.
export const metadata = { title: 'Option card — Design system' };

export default function OptionCardLayout({ children }: { children: React.ReactNode }) {
  return children;
}
