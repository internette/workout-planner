// The page is a client component (its examples keep state), so its tab title is set here.
export const metadata = { title: 'Rating — Design system' };

export default function RatingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
