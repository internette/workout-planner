// The page is a client component (its examples keep state), so its tab title is set here.
export const metadata = { title: 'Chip — Design system' };

export default function ChipLayout({ children }: { children: React.ReactNode }) {
  return children;
}
