// The page is a client component (its examples keep state), so its tab title is set here.
export const metadata = { title: 'Checkbox — Design system' };

export default function CheckboxLayout({ children }: { children: React.ReactNode }) {
  return children;
}
