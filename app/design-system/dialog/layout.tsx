// The page is a client component (its examples keep state), so its tab title is set here.
export const metadata = { title: 'Dialog — Design system' };

export default function DialogLayout({ children }: { children: React.ReactNode }) {
  return children;
}
