// The page is a client component (its examples keep state), so its tab title is set here.
export const metadata = { title: 'Popover — Design system' };

export default function PopoverLayout({ children }: { children: React.ReactNode }) {
  return children;
}
