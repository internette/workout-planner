// The page is a client component (its examples keep state), so its tab title is set here.
export const metadata = { title: 'Reorderable list — Design system' };

export default function ReorderableListLayout({ children }: { children: React.ReactNode }) {
  return children;
}
