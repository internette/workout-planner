// The page is a client component (its examples keep state), so its tab title is set here.
export const metadata = { title: 'Text field — Design system' };

export default function TextFieldLayout({ children }: { children: React.ReactNode }) {
  return children;
}
