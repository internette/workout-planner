export const metadata = { title: 'Spellbook — Moonshot' };

// A saved workout's own address. The planner in the layout reads the id from it and opens that workout.
export default function Page() {
  return null;
}
