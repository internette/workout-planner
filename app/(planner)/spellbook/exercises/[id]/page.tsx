export const metadata = { title: 'Spellbook — Moonshot' };

// An exercise's own address. The planner in the layout reads the id from it and opens that exercise.
export default function Page() {
  return null;
}
