export const metadata = { title: 'Spellbook — Moonshot' };

// An address inside the planner; the planner in the layout reads it and opens that screen.
export default function Page() {
  return null;
}
